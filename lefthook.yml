pre-commit:
  parallel: true
  commands:
    git-secrets:
      run: git secrets --pre_commit_hook -- {staged_files}
    fix:
      glob: "*.{js,ts,jsx,tsx}"
      run: pnpm run fix
      stage_fixed: true

pre-push:
  commands:
    test:
      run: pnpm run test
